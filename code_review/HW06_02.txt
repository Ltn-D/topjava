jsp
    -- c <base href="http://localhost:8080/topjava/"> будет работать ограниченно.
    А ${pageContext.request.contextPath} - универсально
    -- в пути к css тоже убери оставь только путь относительно base
• DataJpaUserRepository#getWithMeals - тут только немодифицирующие запросы,
поэтому полноценная транзакция не нужна, достаточно readOnly
    -- 2 запроса с join: user + roles и user + meals. Однако после первого запроса достаточно достать только еду юзера - join лишний
• JdbcUserRepository
    -- приватные методы располагают после неприватных, в которых используются, т. е. insertRoles - после save.
    И остальные методы тоже должны располагаться в каком-то объяснимом порядке.
    Раньше располагались модифицирующие методы, потом - методы чтения.
    -- стр. 87-88 - между методами должна остаться одна пустая строка
• AbstractMealController, стр. 24
    -- повтори методы класса Object
    -- расширь область видимости логгера, чтобы наследники использовали его же
• spring-db - <tx:annotation-driven /> теперь нужно для всех профилей, вынеси в общую часть
• AbstractUserServiceTest#getAll, стр. 77 - ожидания неверные, роли юзера должны всегда доставаться с ним
(они нужны в таблице юзеров, попробуй запустить приложение с профилем jdbc).
Прошлая реализация JdbcUserRepository#getAll отрабатывали функционально верно, только в реализации надо было избежать выполнения N+1 запроса в БД
