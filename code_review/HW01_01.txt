• сделай так, чтобы с главной были ссылки users, meals (без action)

• Meal - в классе располагают сначала поля, потом конструкторы (из них конструктор без параметров обычно первый), потом остальные методы

MealTo
• тут id всегда будет установлен, поэтому тип - достаточно примитива
• стр. 17-19 - пустые строки оставляют по одной
• getDateTime
10. Не размещайте никакую логику (форматирование, счетчики) в бинах, где хранятся только данные (`Meal`, `MealTo`)
И если где-то понадобится фиксированный паттерн, то для него лучше сделать константу, а не создавать новый DateTimeFormatter.ofPattern(...) при каждом использовании

MealsRepository
• сделай автоформат (Ctrl+Alt+L), см. разницу
• deleteByID -> deleteById
• в операциях сохранения (create/update/save) принято возвращать сохраненный объект
• в параметрах методов недопустим mealId, равный null -> сделай параметр примитивом
• mealId -> id ("meal" есть в имени класса, по умолчанию все про "meal")

InMemoryMealsRepositoryImpl
• не надо Impl в реализациях, если можно указать в имени ее особенность (тут - указана InMemory)
• meals - см. в java.util.concurrent потокобезопасные реализации коллекций, они эффективнее, чем synchronized...
• добавляй тестовые данные через свой метод save, там есть вся логика для этого
• findAll - отдаешь ссылку на коллекцию-хранилище, это небезопасно. Так внешний код сможет ее изменить напрямую
• findById - будет NoSuchElementException, если такая еда не найдется. В этом случае метод должен не бросаться исключениями, а вернуть null
• save (ветка создания)
    -- реализация не потокобезопасная. Если между стр. 43 и 44 другой поток добавит запись,
       то у создаваемых записей будут одинаковые id. И перебирать всю коллекцию для генерации нового id слишком накладно.
       К тому же, идентификаторы должны быть уникальными, а при таком подходе новые id могут повторять id удаленных записей, что неверно.
       Используй отдельную последовательность целых чисел, бери из нее каждый раз следующее.
    -- стр. 41-42 - этот код уйдет, но было бы проще так: .mapToInt(Meal::getId).max()
• save (ветка апдейта)
    -- реализация не потокобезопасна. Пока поля будут сеттиться по одному, другой поток уже может считать недоделанный объект.

• обрати внимание, что доступ к элементам по id, и для этого приходится перебирать всю коллекцию. List - не лучший выбор коллекции для хранения, поищи более удобную структуру.

MealsUtil
• caloriesPerDay - в Java "константы" - _static final deeply immutable_ - именуют верхним регистром с разделением слов через _
• стр. 15-16 - между полем и методом должна быть одна пустая строка. Но вообще метод main больше не нужен, его можно удалить
• getAll
    -- LocalTime.of(23, 59) - не самый конец дня. См. константы в LocalTime
    -- норма калориев у каждого пользователя будет своя (см. демо). 2000 - это временная мера. Поэтому метод getAll(List<Meal> meals),
    работающий только с caloriesPerDay, приложению не нужен. Принимай норму калориев в параметрах.

jsp
• стр. 1-7 - шаблон по умолчанию от Idea - не надо
• meals.jsp, стр. 38-40 - стиль можно применить ко всей строке

MealServlet
• repository
-- сделай private
-- инициализацию экземпляра сервлета (нестатических полей) принято делать в методе HttpServlet#init
• doGet - вместо if/else просится switch
• System.out не используй, пиши в лог
• doPost - dateTime из запроса почему-то считывается совсем отдельно. Или заинлийнь, или считывай все параметры где-то кучно