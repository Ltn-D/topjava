• app_ru.properties сделай в кодировке UTF-8. Сейчас там ASCII-коды - https://github.com/Ltn-D/topjava/blob/HW10/config/messages/app_ru.properties
• при дублирующейся почте в своем профиле/при регистрации пользователь должен остаться в форме,
а сообщение получить, как другие ошибки валидации
• ExceptionInfoHandler#uiControllerValidationError - отдавать несколько ошибок валидации
дной строкой с разделением через \n (или любой другой разделитель) - не лучший вариант, т. к. клиенту придется
парсить строку, разбивая ее на части. Удобнее было бы, если бы каждая ошибка была бы отдельной единицей информации
• AbstractMealServiceTest#createWithException, AbstractUserServiceTest#createWithException - добавились кейсы с невалидным полем
плюс дубликат. Правда, что дубликат, не очевидно, т. к. выглядит как простое совпадение.
К кейсам вопросы: если дубликаты email и dateTime - не случайность, стоит их взять из других эталонных данных,
чтобы было понятно, что такое уже есть. Непонятно, для чего эти кейсы, т. к. очень напоминают уже имеющиеся -
с пустым именем и описанием
• AdminRestControllerTest#createWithDuplicateEmail - .header("Accept-Language", "ru") сейчас ничего не делает, т. к. отрабатывает CookieLocaleResolver.
Если хочешь определенную локаль для REST API, есть варианты: или передавать параметр, на который отработает LocaleChangeInterceptor, или
у localeResolver не задавать defaultLocale, тогда локаль возьмется из заголовка