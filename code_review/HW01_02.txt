MealTo
• стр. 18 - будет NPE, если id придет null. Поменяй на примитивы и параметр в конструкторе, и тип в геттере

InMemoryMealsRepository
• форматирование - сделай Ctrl+Alt+L, посмотри разницу
• meals - объявляй переменные через наиболее общий подходящий интерфейс (тут - Map)
• стр. 19-25 - "выполнить одно и то же действие для нескольких объектов" - явно напрашивается цикл
• findAll - нет смысла оборачивать list в unmodifiableList, т. к. объект, на который ссылается list, уже оторван от meals,
и изменение этого объекта ни на что не повлияет.
• save
    -- переменную isCreate можно сразу инициализировать правильно в стр. 42
    -- если на апдейт передать meal с несуществующим id, еда будет создана с id в обход счетчика, и потом может быть затерта новой (когда счетчик дойдет до этого значения)
    -- если при апдейте записи с таким id не было и апдейт не случился, возвращай null

MealsUtil
• стр. 15 - похоже, тут caloriesPerDay автоматом в верхний регистр поменялось. Верни, как было, параметр - не константа
• ненужный код не комментируй, а удаляй

MealServlet
• стр. 26-27 - между полем и методом должна быть одна пустая строка
• удаляй ненужный код
• doPost - dateTime из запроса почему-то считывается совсем отдельно (стр. 109), а остальные параметры - в стр. 115.
Или заинлайнь переменную dateTime (в Idea Ctrl+Alt+N на переменной - попробуй), или считывай все параметры где-то кучно (перед Meal meal = new Meal(...))