CrudMealRepository - посмотри в логе, какие запросы генерируются для методов с "магическими" именами. Там есть лишнее.
• CrudUserRepository#getWithMeals - методы чтения не нуждаются в полноценной транзакции, достаточно транзакции только для чтения (readOnly)
• DataJpaUserRepository, стр. 34-36 - оставь одну

DataJpaMealRepository
• где-то выполняется несколько обращений к БД в отдельных последовательных транзакциях. А должно быть - в одной общей
6: Проверьте, что в `DataJpaMealRepository` все обращения к DB для одной операции выполняются в одной транзакции.
(`<logger name="org.springframework.orm.jpa" level="debug"/>` для логирования информации по транзакциям)
• get - см. Optional#filter или возвращай из crudMealRepository не Optional
• стр. 64-65 лишние

• JdbcMealRepository. Делать код так зависимым от активного профиля - плохо. Причем обрати внимание, что код для разных веток почти одинаковый.
 В чем же отличие?
Цель задания была - потренироваться с паттерном шаблонный метод. Посмотри, что такое шаблонный метод.
Вынеси это отличие разных реализаций в отдельный метод - он и будет абстрактным и реализуется по-своему у конкретных классов.

• MealRepository - @Profile(Profiles.DATAJPA) тут ничего не делает.
На уровне метода эта аннотация применяется в java-конфигурациях, когда метод помечен `@Bean` (чтобы этот бин попал или не попал в контекст)

spring-db.xml
• entityManagerFactory и все для транзакций - относится только к jpa и datajpa

тесты
• DataJpa...ServiceTest - поле с сервисом есть в родительских классах, используй его (расширь область видимости)
• getWithMeals - протестируй для юзера, у которого нет еды
7: ...Учесть, что у юзера может отсутствовать еда.
• JdbcMealServiceTest - профиль указан неверный
• AbstractMealServiceTest - стр. 24, 58, 67 лишние

*п. 3.2*
• сводка после класса накапливается, посмотри вывод последнего тестового класса
> 8: Проверьте, что ... итоги тестов класса выводятся корректно *(не копятся).*